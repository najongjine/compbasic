* TODAY()
* DATE(년, 월, 일)
* YEAR(날짜)
* WORKDAY(시작 날짜, 일수, 휴일 날짜)    =WORKDAY(A2, 5)  주말 토요일, 일요일은 빼고 5근무일 뒤 날짜
* SUM
* COUNT, COUNTA
* MAX
* MIN
* =IF(조건, 맞을 때 값, 틀릴 때 값)
* =IF(AND(B2>=60,C2>=60),"합격","불합격")
* =IF(OR(B2>=60,C2>=60),"재시험 제외","재시험")
* =COUNTIF(범위, 조건)
* =AVERAGE(B2:D2)
* =RANK.EQ(B2,$B$2:$B$10,0)
* =SUMIF(조건범위, 조건, 합계범위)
* 정렬 = 데이터를 원하는 순서대로 줄 세우는 것
  데이터->정렬,  순번을 먼저 만들어놔야 오리지널로 복구 가능
* 필터
 데이터->필터
* 조건부 서식 = 조건에 맞는 셀을 자동으로 색칠하기
  홈->조건부서식